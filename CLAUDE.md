# CLAUDE.md — 이 저장소에서 일하는 규칙

AI 도구(Claude Code)와 함께 만들었습니다. 이 파일은 **다음에 이 코드를 만지는 사람과 에이전트**를 위한 것입니다.
무엇을 사람이 정했고 무엇을 AI가 썼는지, 그리고 여기서 지켜야 할 원칙이 무엇인지 적습니다.

---

## 1. 누가 무엇을 했는가

커밋 저자는 전부 한 사람이고 대부분에 `Co-Authored-By: Claude` 가 붙어 있어,
기록만으로는 역할이 갈리지 않습니다. 실제 분담은 이랬습니다.

### 사람이 정한 것 (AI 가 제안했어도 채택·기각은 사람이 했습니다)

| 결정 | 내용 |
|---|---|
| 도메인 | AI 뉴스 → **문화·공간**. 과제가 금지한 6개 매체와 겹쳐서 |
| 독자 | "화제작보다 덜 알려진 것을 선호" · "이미 아는 것" 을 따로 정의 |
| 묶음의 기준 | 분류가 아니라 **행동** 으로 — 집에서 보는 것 / 찾아가는 것 |
| 방향 전환 | "주말 계획" → **"교양"**. 중요도 기준 1·2번 교체 |
| 국내 비중 | "국내 사용자 대상인데 해외는 참여 제약이 있다" → 국내 3곳 → 8곳 |
| 쿼터 | 실측 비율에 맞춰 문화 3·공간 2 → **문화 2·공간 3** |
| 이름·문구 | `오늘의 곁눈질` · `{날짜} · {배분}` · `오늘의 픽!` |
| 비교 실험 진행 여부 | 세 번 모두 "재 보자" 는 사람 판단 |
| 발행·공개·제출 | `--send`, Public 전환, 제출 — 전부 사람이 승인 후 실행 |

### AI 가 한 것

파이프라인 구현, 소스 실측 스크립트, 비교 실험 설계와 집행, 문서화,
그리고 **자기가 쓴 코드의 결함을 찾아 보고하는 일**.
아래 5절의 결함 목록은 대부분 AI 가 스스로 찾아 보고한 것입니다.

### 사람이 개입해 방향을 바꾼 지점

- **"공공 데이터 API 를 온라인에서 쓸 방법을 찾아라"** → CI 캐시 설계로 이어짐
- **"문화 쪽 국내 소스를 더 찾아라"** → 40여 곳 추가 실측, 독서신문 한 곳만 건짐
- **"비교 실험 해줘"** → 세 번 중 한 번은 *"바꾸지 않는다"* 가 결론
- **"불필요한 건 모두 정리"** → 죽은 코드 제거 중 `tz_offset` 버그 발견

---

## 2. 이 저장소의 원칙

고칠 때 이 다섯 가지를 깨지 마세요. 전부 **깨져 본 뒤에** 생긴 규칙입니다.

### ① 부탁이 아니라 코드·스키마로 강제한다

프롬프트에 적은 것은 지켜질 때도 있고 안 지켜질 때도 있습니다.
**"이 지시가 지켜졌는지 출력만 보고 확인할 수 있는가?"** — 확인할 수 있으면 코드로 옮깁니다.

- 토픽 → 스키마 `enum` (기본값을 주면 모델이 통째로 생략합니다)
- 문체 → 문장 끝을 검사해 `style_tries` 까지 재요청
- 같은 사건 → `event` 라벨을 세고, 제목의 희귀 낱말로 한 번 더 확인
- 탈락 사유 → `Drop.reason` 을 **필수 칸**으로

### ② 보장은 결과가 정해지는 자리에서 건다

쿼터를 `select()` 에 걸었더니 그 뒤 `verify()` 가 건수를 줄여 무너졌습니다.
**7회 중 6회 배분이 깨졌습니다.** 지금은 `pick_for_publish()` 에서 다시 겁니다.

### ③ 판단에는 사유를 남긴다

채택(`＋`)·탈락(`−`)·재요청·캐시 나이·라벨 수가 전부 로그에 남고, 로그는 `metrics.jsonl` 에 통째로 들어갑니다.
**로그에 안 남는 판단은 없는 것과 같습니다.**

### ④ 설정과 코드의 경계

분야가 바뀌면 바뀌는 값은 전부 YAML 입니다 — 독자·기준·버릴 것·토픽·소스·발행 문구.
`graph.py` 에는 절차만 남깁니다.

> ⚠️ 설정 문자열에 **조사를 붙이지 마세요.** 값이 무엇으로 끝날지 알 수 없습니다.
> `f"{독자.누구}을 위한"` 이 실제로 **"선호을 위한"** 이 됐습니다. `[독자] …` 처럼 한 줄을 따로 주세요.

### ⑤ 관문과 내용 판단은 다른 층이다

| 층 | 무엇을 묻나 | 어디서 |
|---|---|---|
| 관문 G1·G2·G3 | **쓸 수 있는가** (본문·생존·접근) | `source_audit.py` · 결정론적 |
| 버릴 것 | **쓸 만한가** | 선별 단계 LLM |
| 검수 | **원문과 맞는가** | 생성과 분리된 별도 호출 |

검수는 *"원문에 진짜 있는 사실"* 은 통과시킵니다. 그래서 `버릴_낱말` 층을 발행 직전에 하나 더 뒀습니다.

---

## 3. 프롬프트를 고치기 전에

**반드시 재고 고칩니다.** 검수 프롬프트를 '개선' 하려다 오탐을 0/3 → 3/3 으로 만든 적이 있습니다.

> 고정 입력을 떠 둔다 → 정답을 손으로 붙인다 → 변형별로 여러 번 돌린다
> → **고치려던 것과 함께 망가질 수 있는 것도 잰다.**

마지막 조건이 핵심입니다. 그때 "환각을 더 잘 잡는가" 만 보고 **"멀쩡한 것을 떨어뜨리지 않는가"** 를 안 봤습니다.

```bash
./.venv/bin/python experiments/exp_dup.py --detector-only   # 키 없이 즉시 재현
./.venv/bin/python experiments/exp_drops.py --reps 6
```

고정 입력과 정답표는 `experiments/_common.py` 에 있습니다. 자세한 것은 `experiments/README.md`.

### 건드리기 전에 생각할 것

- **`check()` 의 세 줄** — 비교 실험의 결과입니다. 길게 늘이면 오탐이 늘어납니다.
- **본선 요청 수(`OVERSELECT * 2`)** — 줄여 봤고, 쿼터 충족만 47%→33% 로 나빠졌습니다. 그대로 두세요.
- **데스크지침에 "반드시 적을 것"** — 원문에 없을 때 지어내라는 말과 같습니다. 없는 러닝타임 100분을 만든 적이 있습니다.

---

## 4. 작업 습관

- **커밋 메시지는 "무엇을" 이 아니라 "왜"** 를 적습니다. 실측 숫자가 있으면 함께.
- 수치를 문서에 쓸 때는 **기억이 아니라 `store/` 에서** 꺼냅니다. 한 번 회차 번호를 잘못 옮겨 적은 적이 있습니다.
- 변경 뒤에는 `./.venv/bin/python run.py --dry-run` 으로 전 구간을 돌려 봅니다.
- **되돌리기 어려운 것은 사람에게 묻습니다** — `--send`, 저장소 공개, 제출.

---

## 5. 알려진 결함과 한계

숨기지 않고 적습니다. 자세한 것은 `REPORT.md` 6절.

- **`verify_fail` 이 0 인 것은 좋은 소식이 아닙니다** — 검수가 일하는지 확인이 필요하다는 신호입니다.
- **검수기 자체의 평가셋이 없습니다.** 실험은 전부 선별 단계입니다.
- **`experiments/` 는 회귀 검사가 아닙니다** — 통과 문턱도, 자동 실행도 없습니다. 사람이 돌리고 사람이 읽습니다.
- **문화가 공간에 구조적으로 밀립니다** — 전용 소스 수는 비슷한데 기여가 3배 차이입니다. 중요도 기준을 전시 기사가 더 잘 만족하기 때문이고, 소스를 늘려서 풀리지 않습니다.
- **낱말로 안 잡히는 버릴 것**(예: "보도자료 표현만 있는 글")은 여전히 선별만 봅니다.
- **국내 공공 API 는 CI 에서 닿지 않습니다.** 캐시로 우회하고 있고, 캐시가 시간 창보다 오래되면 저절로 만료됩니다.

---

## 6. 키 없이 확인할 수 있는 것

`OPENAI_API_KEY` 가 있어야 파이프라인이 돕니다. 키 없이 볼 수 있는 것은 이렇습니다.

```bash
./.venv/bin/python experiments/exp_dup.py --detector-only   # 중복 검출기 210쌍 전수 평가
./.venv/bin/python report_card.py                            # 쌓인 실행 기록으로 성적표
cat store/metrics.jsonl | tail -1                            # 마지막 회차의 전 구간 로그
```
